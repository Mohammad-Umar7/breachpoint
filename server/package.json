{
  "name": "breachpoint-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Free-for-all game server for Breachpoint. Imports the client's own weapon definitions and arena data so the two can never drift.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
